{
  "name": "redde-functions",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "deploy": "firebase deploy --only functions",
    "lint": "echo \"(add linter)\""
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.8.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}